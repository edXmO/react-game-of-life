import React from "react";
import styled from "styled-components";

/**
 * COMPONENTS
 */
import TweakBtn from "./TweakBtn";

const TweakWrapper = style.div`
`

const TweakPanel = () => {
    // Panel para retocar colores, velocidad/ tama;o del grid
    return;
}

export default TweakPanel;